name: coa2

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: "coa2"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    ports:
      - "5432:5432"